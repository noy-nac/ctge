
namespace Graphics.Raster.Rendering {
	public class Loop {

		private Sprite[] loop;

		public Loop(byte size) {
			Initalize(size);
		}

		public void Initalize(byte size) {
			loop = new Sprite[size];
		}

		public void Add(Sprite sprite, byte pos) {
		}

	}
}
